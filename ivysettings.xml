<ivysettings>
	<settings defaultResolver="localChain" />
	
	<property name="ivy.local.default.root"
              value="http://ivy.procippus.com/"
              override="false" />

	<property name="ivy.local.default.ivy.pattern"
		  value="[organisation]/[module]/[revision]/[artifact].[ext]"
		  override="false" />

	<property name="ivy.local.default.artifact.pattern"
		  value="[organisation]/[module]/[revision]/[artifact].[ext]"
		  override="false" />

	<resolvers>
		<url name="public">
			<ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
			<artifact pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
		</url>

		<chain name="localChain" returnFirst="true">
			<resolver ref="public" />
		</chain>
	</resolvers>
</ivysettings>
